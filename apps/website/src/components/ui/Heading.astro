---
interface Props {
    classes?: string;
    id: string;
}

const { classes, id } = Astro.props;
---

<h2 id={id} class:list={["text-3xl font-black tracking-tight text-white uppercase", classes]}>
    <slot />
</h2>
