---
interface Props {
    classes?: string;
}

const { classes } = Astro.props;
---

<ul class=`${classes} mt-2.5 flex items-center gap-2.5 text-2xl font-bold md:mt-5 md:gap-5 flex-wrap`>
    <slot />
</ul>
