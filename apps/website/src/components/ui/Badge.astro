---
interface Props {
    text: string;
    classes?: string;
}

const { text, classes } = Astro.props;
---

<span class=`bg-carbon-black rounded-full p-1 px-3 font-bold text-white ${classes}`>{text}</span>
